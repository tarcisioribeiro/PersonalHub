asgiref==3.12.1
gunicorn==26.0.0
cryptography==50.0.0
Django==6.0.8
django-admin-dracula==0.0.3
django-restframework==0.0.1
djangorestframework==3.17.2
djangorestframework_simplejwt==5.5.1
pillow==12.3.0
pytesseract==0.3.13
psycopg2-binary==2.9.12
PyJWT==2.13.0
python-dotenv==1.2.2
sqlparse==0.6.0

# Performance and filtering
django-filter==26.1
django-cors-headers==4.9.0
drf-spectacular==0.30.0

# Monitoring and logging
python-json-logger==4.1.0
django-prometheus==2.5.0
sentry-sdk[django]==2.66.1

# HTTP client
requests==2.34.2

# Redis cache
django-redis==7.0.0

# Async task queue
celery==5.6.3
django-celery-beat==2.9.0
redis==8.1.0

# S3-compatible storage (MinIO)
django-storages[s3]==1.14.6
boto3==1.43.59

# Export (PDF/CSV)
reportlab==5.0.0

# AI Assistant - NLP and text processing
rapidfuzz==3.14.5
dateparser==1.4.1

# 2FA / TOTP
pyotp==2.10.0
qrcode==8.2

# Agents / LLM integration
# anthropic: SDK para Claude API (opcional — usado quando LLM_PROVIDER=anthropic)
anthropic==0.120.2
# pgvector: VectorField nativo para Django + pgvector extension no PostgreSQL
pgvector==0.3.3

# Transitive dependency security pins — these aren't imported directly, but
# pinning them here forces the fixed version even when the pulling package
# (redis/celery -> msgpack; pip's build env -> setuptools) hasn't bumped its
# own floor yet. See apps/api/Dockerfile comment on PIP_CACHE_BUST for why
# this is necessary instead of relying on weekly cache-bust re-resolution.
msgpack==1.2.1        # GHSA-6v7p-g79w-8964
setuptools==83.0.0    # CVE-2025-47273, PYSEC-2026-3447
