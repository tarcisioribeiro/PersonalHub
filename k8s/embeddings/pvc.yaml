apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: embeddings-models-pvc
  namespace: personalhub
  labels:
    app.kubernetes.io/name: personalhub
    app.kubernetes.io/component: embeddings
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 500Mi
  storageClassName: standard
